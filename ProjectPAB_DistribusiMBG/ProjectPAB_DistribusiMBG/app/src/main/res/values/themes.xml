<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProjectPAB_DistribusiMBG" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Warna status bar -->
        <item name="android:statusBarColor">@color/white</item>

        <!-- Warna navigation bar -->
        <item name="android:navigationBarColor">@color/white</item>

        <!-- Supaya ikon status bar dan navbar jadi hitam -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.ProjectPAB_DistribusiMBG" parent="Base.Theme.ProjectPAB_DistribusiMBG" />
</resources>